import { CircularProgress } from "@mui/material";
import React from "react";

const Loading = () => {
  return (
    <CircularProgress className="m-auto top-0 bottom-0 right-0 left-0 fixed" />
  );
};

export default Loading;
